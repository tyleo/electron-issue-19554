{
  "name": "issue-19554",
  "private": true,
  "main": "main.js",
  "devDependencies": {
    "electron": "^11.5.0",
    "touch": "^3.1.0"
  },
  "scripts": {
    "build": "yarn nodetouch main.js && yarn nodetouch renderer.js",
    "start": "electron .",
    "debug": "electron --preserve-symlinks --remote-debugging-port=9223 .",
    "build+start": "yarn build && yarn start",
    "build+debug": "yarn build && yarn debug"
  }
}
